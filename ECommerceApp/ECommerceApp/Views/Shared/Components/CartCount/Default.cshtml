@model int
@if (Model > 0)
{
    <span id="cart-count" class="badge bg-danger rounded-pill position-absolute top-25 start-60 translate-middle" style="font-size: 0.65rem; padding: 0.25em 0.5em;">
        @Model
    </span>
}